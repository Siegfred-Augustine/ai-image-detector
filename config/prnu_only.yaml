# Table row: "Single-stream PRNU CNN"
# PRNU branch only — feeds directly into the classifier, no fusion.

model:
  active_branches: [prnu]
  feature_dim: 128
  classifier_hidden_dim: 64
  dropout: 0.5
  num_classes: 2

branch_overrides: {}
  # prnu: {conv1_channels: 32, conv2_channels: 64, kernel_size: 3, stride: 1, padding: 1}

training:
  learning_rate: null
  weight_decay: null
  batch_size: null
  epochs: null
  lr_min: null
