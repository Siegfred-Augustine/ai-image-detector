# Table row: "Single-Stream Content CNN"
# Content branch only — feeds directly into the classifier, no fusion.

model:
  active_branches: [content]
  feature_dim: 128
  classifier_hidden_dim: 64
  dropout: 0.5
  num_classes: 2

branch_overrides: {}
  # content: {block_channels: [32, 64, 128], kernel_size: 3, stride: 1, padding: 1}

training:
  learning_rate: null
  weight_decay: null
  batch_size: null
  epochs: null
  lr_min: null
