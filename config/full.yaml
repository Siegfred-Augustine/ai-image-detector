# Table row: "Proposed Multistream model"
# All three forensic branches + 3-way attention fusion.

model:
  active_branches: [ela, prnu, content]
  feature_dim: 128            # n — NOT specified in research doc, confirm with team
  classifier_hidden_dim: 64   # NOT specified in research doc
  dropout: 0.5                # NOT specified in research doc
  num_classes: 2

# Per-branch overrides (filters, kernel size, etc.) — leave empty to use
# each branch file's defaults. Uncomment/edit once the team confirms values.
branch_overrides: {}
  # ela: {conv1_channels: 32, conv2_channels: 64, kernel_size: 3, stride: 1, padding: 1}
  # prnu: {conv1_channels: 32, conv2_channels: 64, kernel_size: 3, stride: 1, padding: 1}
  # content: {block_channels: [32, 64, 128], kernel_size: 3, stride: 1, padding: 1}

# Training hyperparameters are NOT specified in the research doc
# (Handoff Section 19). Left as null placeholders — this is
# training/train.py's concern, not models/, but recorded here since
# the config file will eventually need them.
training:
  learning_rate: null
  weight_decay: null
  batch_size: null
  epochs: null
  lr_min: null
